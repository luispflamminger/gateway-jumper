include:
- project: dhei/teams/hyperion/dev/src/foundation-build
  file: /pipelines/java-maven-docker-release-pipeline.yaml
  ref: 15.7.0
variables:
  MTR_TARGET_IMAGE: ${CI_PROJECT_NAME}
build docker image:
  extends: .build-docker-image-manual
mtr to harbor:
  extends: .mtr_to_harbor
  needs:
  - job: build_deploy
    artifacts: true
  - job: dockerize
