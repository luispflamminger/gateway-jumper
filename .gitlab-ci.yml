include:
  - project: 'dhei/teams/hyperion/dev/src/foundation-build'
    file: '/pipelines/java-maven-docker-pipeline.yaml'
    ref: main

variables:
  MTR_TARGET_IMAGE: ${CI_PROJECT_NAME}

mtr to harbor:
  extends: .mtr_to_harbor
  needs:
    - job: build_deploy
      artifacts: true
    - job: dockerize
    
